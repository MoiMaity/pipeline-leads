# Port the HTTP server binds to.
PORT=3000
HOST=0.0.0.0

# Where the SQLite file lives. Use :memory: for throwaway runs.
DB_PATH=./data/app.db

# Set to 1 behind a TLS-terminating proxy so cookies are marked Secure
# and X-Forwarded-For is used for rate limiting.
TRUST_PROXY=0
